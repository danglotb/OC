package solver.multiobjectiv

import data.multiobjectiv.PermutationSolution

import scala.collection.mutable.ListBuffer

/**
 * @author danglot
 */
class Filter {

  def filter(solution : List[(Int,Int)]) : List[(Int, Int)] = {
    solution
  }
  
}